/**
 * @hyperslop-systems/workbench-core — the headless PBUI workbench engine.
 *
 * Phase 0 (PBUI-WORKBENCH-CORE-1): the package exists so its React/DOM fence
 * is enforced from the first commit. The engine lands phase by phase.
 */
export {};
